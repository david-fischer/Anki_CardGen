#:import SettingsRoot screens.SettingsRoot
SettingsRoot:
    id: settings_root
    orientation: "vertical"

    MDRaisedButton:
        text: "Theme"
        on_press: root.theme_dialog.open()

    MDRaisedButton:
        text: "Busy"
        on_press: root.sleep_some()
